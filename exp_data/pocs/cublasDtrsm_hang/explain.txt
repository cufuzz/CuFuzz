1. numElements =178； blockSize=234；在终端直接执行，会导致程序长时间卡住。怀疑numElements<blockSize导致的，但是num取50， block取60不会卡住。
2. 使用cuda-gdb执行程序，不会卡住，只有终端直接执行会卡住。
3. 没有查看函数签名，但是猜测跟cublasDgemm一样，不是函数参数的问题。
4. 去掉UT查看，同样存在hang的问题。

在127（565，12.7）上不会卡住。在124（550，12.4）会卡